%h1 Admin::Dashboard#index
%p Control everything in your app

%p 
	Listing all projects:
	-@projects.each do |project|
		=link_to project.title, edit_admin_project_path(project.id)
	%p 
		=link_to 'Create new project', new_admin_project_path

%p Listing all services

%p Listing teammembers
